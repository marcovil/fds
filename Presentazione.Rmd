---
title: "New York Philharmonic"
author: "Marco Villotta"
date: "2023-06-22"
output:
  ioslides_presentation:
    widescreen: true
    smaller: true
---
<style type="text/css">

h2 {
  text-align: center;
}
}
h1 {
  text-align: center;
}
}
</style>

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

```{r, warning=FALSE, message=FALSE}
library(tidyr)
library(dplyr)
library(jsonlite)
library(purrr)
library(stringr)
library(lubridate)
library(modelr)
library(broom)
library(tibble)
library(ggplot2)
library(mgcv)
library(ineq)

performances <- readRDS("perf.RDS")
pcp <- readRDS("pcp.RDS")
pcp_models_simp <- readRDS("pcp_models_simp.RDS")
pcp_glance <- readRDS("pcp_glance.RDS")
pcp_resids <- readRDS("pcp_resids.RDS")
before_1909 <- readRDS("before_1909")
regs <- readRDS("regs.RDS")
ccn2 <- readRDS("ccn2.RDS")
```

## Dataset usato {.flexbox .vcenter}
New York Philharmonic, pubblicato su https://github.com/nyphilarchive/PerformanceHistory e disponibile in XML o JSON (che ho usato).
Orchestra americana, ancora attiva, fondata nel 1842.

```{js echo = TRUE}
  "programs": [
    {
      "id": "38e072a7-8fc9-4f9a-8eac-3957905c0002", // GUID
      "programID": "3853", // NYP Local ID
      "orchestra": "New York Philharmonic",
      "season": "1842-43",
          "concerts": [ { ... }, /* A program can have multiple concerts */ ],
      "works": [ {
          "composerName": "Weber,  Carl  Maria Von",
          "workTitle": "OBERON",
          "conductorName": "Timm, Henry C.",
        "soloists": [ ... ] }, /* a program will usually have multiple works */ { ... }, ] },
    /* more programs */
  ]
```

## Dataset usato {.flexbox .vcenter}
11 orchestre nel database, più eventi senza orchestra. La stragrande maggioranza delle performance sono della New York Philharmonic.

```{r, message=FALSE}
ggplot(performances) +
  geom_bar(aes(y = orchestra)) +
  labs(
    x = "numero di performance",
    y = NULL,
  ) +
  theme_light()
```

## Dataset usato {.flexbox .vcenter}
La New York Philharmonic è quella più presente nel corso della storia.

```{r}
ggplot(performances) +
  geom_point(aes(seasonYear, orchestra), alpha = 1/4)  +
  labs(
    x = "stagione",
    y = NULL,
  ) +
  theme_light()
```


```{r}
performances <- performances %>%
filter(orchestra == "New York Philharmonic")
```

## Compositori nel tempo {.flexbox .vcenter}
Come evolve negli anni (nel lungo periodo) il repertorio dell'orchestra?
Ci sono compositori per cui cresce o diminuisce il numero di rappresentazioni?

```{r}
ggplot(pcp,aes(seasonYear, perc)) +
  geom_line(aes(color = composerName), alpha = 1/4, show.legend = FALSE) +
  labs(
    x = "stagione",
    y = "performance compositore / performance stagione (%)",
  ) +
  theme_light()
```

## Compositori nel tempo {.flexbox .vcenter}
Componenti quadratiche positive (Dvorak, Strauss) e negative (Ravel, Prokofiev).

```{r, message=FALSE}
pcp %>%
  filter(composerName == "Dvorak, Antonín" | composerName == "Ravel, Maurice" |
           composerName == "Strauss, Richard" | composerName == "Prokofiev, Sergei") %>%
ggplot(aes(seasonYear, perc)) +
  geom_line(show.legend = FALSE) +
  geom_point(show.legend = FALSE) +
  stat_smooth(se=F, method='lm', formula=y~poly(x,2)) +
  labs(
    x = "stagione",
    y = "performance compositore / performance stagione (%)",
  ) +
  facet_wrap(~composerName) +
  theme_light()
```

## Compositori nel tempo {.flexbox .vcenter}
Alcuni modelli che hanno un buon fit.

```{r, message=FALSE}
pcp %>%
  filter(composerName == "Chopin, Frédéric" | composerName == "Bruch, Max" |
           composerName == "Weber, Carl Maria Von" | composerName == "Gluck, Christoph Willibald") %>%
ggplot(aes(seasonYear, perc)) +
  geom_line(show.legend = FALSE) +
  geom_point(show.legend = FALSE) +
  stat_smooth(se=F, method='lm', formula=y~poly(x,2)) +
  labs(
    x = "stagione",
    y = "performance compositore / performance stagione (%)",
  ) +
  facet_wrap(~composerName) +
  theme_light()
```

## Compositori nel tempo {.flexbox .vcenter}
Guardo ai compositori che hanno residui più alti di 0.1 (nel plot la percentuale).

```{r, message=FALSE}
highres <- pcp_resids %>%
  filter(resid > 0.1 | resid < -0.1) %>%
  distinct(composerName)

pcp %>%
  semi_join(highres) %>%
  ggplot() +
  geom_point(aes(x = seasonYear, y = perc),alpha = 1/4) +
  facet_wrap(~composerName) +
  labs(
    x = "stagione",
    y = "performance compositore / performance stagione (%)",
  ) +
  theme_light()
```

## Compositori nel tempo {.flexbox .vcenter}
Le stagioni prima del 1909 sono molto più "eque" nella divisione delle performance fra diversi compositori

```{r, message=FALSE}
pcp %>%
  select(seasonYear,n) %>%
  group_by(seasonYear) %>%
  mutate(gini = Gini(n)) %>%
  distinct(seasonYear,gini) %>%
ggplot() +
    geom_vline(aes(xintercept = 1909, color = "red"), show.legend = FALSE) +
    annotate("text", x = 1917, y = 0.06, label="1909", angle=0) +
    geom_line(aes(seasonYear,gini)) +
  labs(
    x = "stagione",
    y = "indice Gini",
  ) +
  theme_light()
```

## Compositori nel tempo {.flexbox .vcenter}
Questo nonostante aumenti il numero di compositori.

```{r, message=FALSE}
performances %>%
  group_by(seasonYear) %>%
  mutate(composers = n_distinct(composerName)) %>%
  ungroup() %>%
ggplot() +
    geom_vline(aes(xintercept = 1909, color = "red"), show.legend = FALSE) +
    annotate("text", x = 1917, y = 0.06, label="1909", angle=0) +
    geom_line(aes(seasonYear,composers)) +
  labs(
    x = "stagione",
    y = "numero di compositori rappresentati nella stagione",
  ) +
  theme_light()
```

## Compositori nel tempo {.flexbox .vcenter}
Mediana percentuali.

```{r, message=FALSE}
pcp %>%
  group_by(seasonYear) %>%
  mutate(median = median(perc)) %>%
  ggplot() +
  geom_line(aes(seasonYear,median)) +
  geom_vline(aes(xintercept = 1909, color = "red"), show.legend = FALSE) +
  annotate("text", x = 1917, y = 0.06, label="1909", angle=0) +
  labs(
    x = "stagione",
    y = "mediana percentuale compositore/stagione",
  ) +
  theme_light()
```

## Compositori nel tempo {.flexbox .vcenter}
Compositori rappresentati prima della stagione 1909-1910 per almeno dieci stagioni.

```{r, message=FALSE}
ggplot(
  pcp %>%
  inner_join(before_1909)
) +
  geom_point(aes(x = seasonYear, y = perc),alpha = 1/4) +
  geom_vline(aes(xintercept = 1909, color = "red"), show.legend = FALSE) +
  facet_wrap(~composerName) +
  labs(
    x = NULL,
    y = NULL,
  ) +
  scale_x_continuous(labels = NULL) +
  theme_light()
```

## Conduttori nel tempo {.flexbox .vcenter}
È possibile individuare dei trend nei repertori dei conduttori?

## Conduttori nel tempo {.flexbox .vcenter}
Leonard Bernstein - alcuni modelli che fittano bene.

```{r}
very_good <- regs %>%
  unnest(glanced) %>%
  filter(r.squared >= 0.925) %>%
select(conductorName, composerName)

ccn2 %>%
  filter(conductorName == "Bernstein, Leonard") %>%
  filter(composerName == "Ives, Charles" | composerName == "Piston, Walter" |
           composerName == "Gershwin, George" | composerName == "Foss, Lukas") %>%
  ggplot(aes(seasonYear,perc), show.legend = FALSE) +
  geom_line() +
  geom_point() +
  geom_smooth(se=F, method='lm', formula=y~poly(x,2)) +
  facet_wrap(~composerName) +
  labs(
    x = "stagioni",
    y = "performance compositore / performance direttore (%)",
  ) +
  theme_light()
```

## Conduttori nel tempo {.flexbox .vcenter}
Leonard Bernstein - alcuni modelli che non fittano bene.

```{r}
ccn2 %>%
  filter(conductorName == "Bernstein, Leonard") %>%
  filter(composerName == "Beethoven, Ludwig van" | composerName == "Mozart, Wolfgang Amadeus" |
           composerName == "Copland, Aaron" | composerName == "Mahler, Gustav") %>%
  ggplot(aes(seasonYear,perc), show.legend = FALSE) +
  geom_line() +
  geom_point() +
  geom_smooth(se=F, method='lm', formula=y~poly(x,2)) +
  facet_wrap(~composerName) +
  labs(
    x = "stagioni",
    y = "performance compositore / performance direttore (%)",
  ) +
  theme_light()
```
